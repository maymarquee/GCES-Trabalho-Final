# Same default credential used by docker-compose.yml/docker-compose.prod.yml — suitable for
# a local/educational cluster. Replace PGPASSWORD (and rotate it) before using a shared cluster.
apiVersion: v1
kind: Secret
metadata:
  name: mkjs-secrets
  namespace: mkjs
type: Opaque
stringData:
  PGPASSWORD: mkjs
